apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "qbuild.fullname" . }}-test-connection"
  labels:
    {{- include "qbuild.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "qbuild.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
