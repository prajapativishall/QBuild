apiVersion: v1
kind: Service
metadata:
  name: {{ include "qbuild.fullname" . }}
  labels:
    {{- include "qbuild.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "qbuild.selectorLabels" . | nindent 4 }}
